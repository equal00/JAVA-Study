package method;

public class Overloading1 {
    public static void main(String[] args) {
        System.out.println("1: " + add(1,2));
        System.out.println("2: " + add(1,2,3));
    }
    public static int add(int a, int b) {
        System.out.println("1번 호출");
        return a +b;
    }

    public static int add(int a, int b, int c) {
        System.out.println("2번 호출");
        return a + b + c;
    }
}

/*
메서드 오버로딩
 : 이름이 같고 매개변수가 다른 메서드를 여러 개 정의하는 것
 : 과적 = 과하게 물건을 담았다 -> 같은 이름의 메서드를 여러 개 정의했다.

 : 메서드의 이름이 같아도 매개변수의 타입 및 순서가 다르면 오버로딩을 할 수 있다. (만환타입 인정 X)

메서드 시그니처 = 메서드 이름 + 매개변수 타입 (순서)
 : 자바에서 메서드를 구분할 수 있는 고유한 식별자나 서명
 : 메서드의 이름과 매개변수 타입으로 구성

 : 메서드 이름이 같아도 메서드 시그니처가 다르면 다른 메서드로 간주 (반환타입 인정 X)

*/
