package cond;

public class Switch1 {
    public static void main(String[] args) {
        int grade = 2;

        int coupon;
        if (grade == 1) {
            coupon = 1000;
        } else if (grade == 2) {
            coupon = 2000;
        } else if (grade == 3) {
            coupon = 3000;
        } else {
            coupon = 500;
        }
        System.out.println("빌급받은 쿠폰: " + coupon);
    }

}

/*
Switch문은 if문을 조금 더 편리하게 사용할 수 있는 기능
차이점
 - if문은 비교연산자 사용 가능
 - Switch문은 단순 비교만 가능

ex)
switch (조건식) ㅖ
    case value1:
        조건식의 결과 값이 value1일 때 실행되는 코드
        break;
    case value2:
        조건식의 결과 값이 value2일 때 실행되는 코드
        break;
    default:
        조건식의 결과 값이 위의 어떤 값에도 해당되지 않을 때 실행되는 코드
 */
